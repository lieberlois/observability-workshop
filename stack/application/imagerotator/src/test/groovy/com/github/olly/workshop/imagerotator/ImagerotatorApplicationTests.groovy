package com.github.olly.workshop.imagerotator

import org.springframework.boot.test.context.SpringBootTest
import spock.lang.Specification

@SpringBootTest
class ImagerotatorApplicationTests extends Specification {

    def "context loads"() {
        when:
        true

        then:
        true
    }
}
